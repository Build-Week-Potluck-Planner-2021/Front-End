import React from "react";
import { Link } from "react-route
// IMPORTING CARD AND OTHER THINGS LATER
const PermitCard = props => {
    console.log( props, "PermitCardProps" );

// RETURN CARD ALONG WITH PERMIT NAME, DATE, ID AND A BUTTON
    return(
        <Card>

        </Card>
    )
}